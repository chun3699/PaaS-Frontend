#อัพด้วย Portainer
version: '3.8'

services:
  frontend:
    container_name: paas_frontend
    
    build:
      # 🔥 สูตรอาจารย์: ใส่ URL Git Frontend + Token
      # Docker จะวิ่งไปดึง Dockerfile และโค้ดมา Build เอง
      context: https://chun3699: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@github.com/chun3699/PaaS-Frontend.git#main
      dockerfile: Dockerfile
      
    restart: unless-stopped
    
    # เปิด Port ออกมา (ถ้าจะเข้าผ่าน IP ตรงๆ)
    # ผมใส่ 3002 ไว้ (เพราะ Backend คุณใช้ 3001 ไปแล้ว)
    # เข้าเว็บผ่าน: http://10.33.4.47:3002
    ports:
      - "3001:3000"
    
    environment:
      # เหลือแค่บอกให้ Nuxt รันที่ Port 3000 (ภายใน Container)
      PORT: 3000


# #อัพด้วย Filezilla
# version: '3.8'

# services:
#   frontend:
#     container_name: paas_frontend
    
#     # 🔥 ชี้ไปที่โฟลเดอร์ปัจจุบัน (ที่คุณลากไฟล์มาลง) แทนการดึงจาก Git
#     build: .
    
#     restart: unless-stopped
    
#     # เปิด Port ออกมา (ถ้าจะเข้าผ่าน IP ตรงๆ)
#     # เข้าเว็บผ่าน: http://<IPของเครื่องคุณ>:3001
#     ports:
#       - "3001:3000"
    
#     environment:
#       # บอกให้ Nuxt รันที่ Port 3000 (ภายใน Container)
#       PORT: 3000